assign 0 to a;
read[b];

while(< a b){
    print[a];
    assign +(a, 1) to a
};

read[c];

conditional[
    option(> c +(a,b)) do {
        assign +(a, b) to d;
        while(< d c){
            print[d];
            assign +(d, 1) to d
        }
    }
] end